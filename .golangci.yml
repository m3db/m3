version: "2"
run:
  build-tags:
    - big
    - compatibility
    - dtest
    - integration
  modules-download-mode: readonly
  issues-exit-code: 1
  tests: true
linters:
  # NB: the following linters are deliberately not enabled. The v1 -> v2
  # migration expands the old `presets` blocks against v2's linter set, which
  # silently pulls in checks this repo never opted into. Adopting them (and
  # fixing the ~561 findings they report) is follow-up work, kept out of this
  # toolchain bump:
  #   new since v1.56:  intrange
  #   rules expanded:   goconst, noctx, prealloc
  # Re-enabled so far: copyloopvar, canonicalheader, nilnesserr, iface,
  # exptostd, predeclared.
  # recvcheck is deliberately left disabled: most of what it reports is the
  # value-receiver Marshal* / pointer-receiver Unmarshal* pairing that YAML and
  # JSON round-tripping requires, and the rest is not worth the churn.
  enable:
    - asasalint
    - canonicalheader
    - copyloopvar
    - exptostd
    - iface
    - nilnesserr
    - predeclared
    - asciicheck
    - bidichk
    - decorder
    - dupl
    - durationcheck
    - errchkjson
    - errorlint
    - exhaustive
    - forbidigo
    - ginkgolinter
    - gocheckcompilerdirectives
    - gochecksumtype
    - gocritic
    - goheader
    - gomodguard_v2
    - goprintffuncname
    - grouper
    - importas
    - lll
    - loggercheck
    - makezero
    - misspell
    - nakedret
    - nosprintfhostport
    - reassign
    - rowserrcheck
    - sloglint
    - spancheck
    - sqlclosecheck
    - tparallel
    - unconvert
    - unparam
    - zerologlint
  disable:
    - bodyclose
    - containedctx
    - contextcheck
    - depguard
    - dogsled
    - err113
    - errname
    - exhaustruct
    - forcetypeassert
    - funlen
    - gochecknoglobals
    - gochecknoinits
    - gocyclo
    - godot
    - godox
    - gomoddirectives
    - gosec
    - gosmopolitan
    - inamedparam
    - interfacebloat
    - ireturn
    - mirror
    - mnd
    - musttag
    - nilerr
    - nilnil
    - nlreturn
    - nolintlint
    - nonamedreturns
    - paralleltest
    - promlinter
    - protogetter
    - recvcheck
    - revive
    - tagalign
    - tagliatelle
    - testifylint
    - testpackage
    - thelper
    - usestdlibvars
    - varnamelen
    - wastedassign
    - whitespace
    - wrapcheck
    - wsl
  settings:
    depguard:
      rules:
        main:
          deny:
            - pkg: github.com/sirupsen/logrus
              desc: logging is allowed only by logutils.Log
            - pkg: github.com/golang/protobuf/jsonpb
              desc: replace with github.com/gogo/protobuf/jsonpb
            - pkg: google.golang.org/protobuf/encoding/protojson
              desc: replace with github.com/gogo/protobuf/jsonpb
            - pkg: github.com/golang/protobuf/proto
              desc: replace with github.com/gogo/protobuf/proto
            - pkg: google.golang.org/protobuf/proto
              desc: replace with github.com/gogo/protobuf/proto
            - pkg: github.com/tj/assert
              desc: use github.com/stretchr/testify/assert
    dupl:
      threshold: 100
    exhaustive:
      default-signifies-exhaustive: true
    goconst:
      min-len: 3
      min-occurrences: 3
    gocritic:
      disabled-checks:
        - hugeParam
        - rangeValCopy
        - ifElseChain
      enabled-tags:
        - performance
      settings:
        captLocal:
          paramsOnly: true
    govet:
      enable:
        - shadow
      disable:
        # new analyzer, did not run under v1.56
        - inline
    lll:
      line-length: 120
      tab-width: 1
    misspell:
      locale: US
      ignore-rules:
        - someword
    nakedret:
      max-func-lines: 30
    prealloc:
      simple: true
      range-loops: true
      for-loops: false
    staticcheck:
      checks:
        - all
        - -SA1019
        # stylecheck was merged into staticcheck in v2 but is disabled above;
        # QF is a new family. Neither ran under v1.56.
        - -ST*
        - -QF*
        # S1004/S1009 report pre-existing code that v1.56 did not flag. Like
        # the linters left out of `enable` above, adopting them is follow-up
        # work kept out of this toolchain bump.
        - -S1004
        - -S1009
    unparam:
      check-exported: false
  exclusions:
    generated: lax
    rules:
      - linters:
          - lll
        source: '^//go:generate '
      - path: (.+)\.go$
        text: Using the variable on range scope `tt` in function literal
      - path: (.+)\.go$
        text: Using the variable on range scope `test` in function literal
      - path: (.+)\.go$
        text: 'shadow: declaration of "err" shadows declaration'
      - path: (.+)\.go$
        text: Profiling endpoint is automatically exposed on /debug/pprof
      - path: (.+)\.go$
        text: 'Blacklisted import `crypto/md5`: weak cryptographic primitive'
      - path: (.+)\.go$
        text: Error return value of `\(github.com\/go-kit\/kit\/log.Logger\).Log` is not checked
      - path: (.+)\.go$
        text: Error return value of `[a-zA-Z.]+.Body.Close` is not checked
      - path: (.+)\.go$
        text: 'G104: Errors unhandled'
      - path: (.+)\.go$
        text: 'G404: Use of weak random number generator (math/rand instead of crypto/rand)'
      - path: (.+)\.go$
        text: 'G103: Use of unsafe calls should be audited'
      - linters:
          - goconst
        path: (.+)_test\.go
    paths:
      - .*_gen.go$
      - .*\.peg\.go$
      - generated/.*
      - eventlog/test$
      - third_party$
      - builtin$
      - examples$
issues:
  max-issues-per-linter: 0
  max-same-issues: 0
  new-from-rev: 30e1c10d456af9d3778288cfb7c247ecd06a4339
formatters:
  enable:
    - gci
    - gofmt
    - goimports
  settings:
    gci:
      sections:
        - standard
        - prefix(github.com/m3db/m3)
        - default
  exclusions:
    generated: lax
    paths:
      - .*_gen.go$
      - .*\.peg\.go$
      - generated/.*
      - eventlog/test$
      - third_party$
      - builtin$
      - examples$
