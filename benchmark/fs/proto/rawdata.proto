syntax = "proto3";
package memtsdb.rawdatadump;

message DataEntry {
    string id = 1;

    message Datapoint {
        int64 timestamp = 1;
        double value = 2;
    }

    repeated Datapoint values = 2;
}
